export const tailwindBlueprint = {
  framework: 'vite',
  language: 'typescript',
  styling: 'tailwind',
  themeExtension: true,
  darkMode: 'class',
  plugins: [],
  productionReady: true,
  filePurpose: 'Configure Tailwind CSS for a modern, responsive Vite + React + TypeScript project.',
  instructions: [
    "Use darkMode: 'class'",
    "Include ./index.html and all src/**/*.{ts,tsx,jsx,js} files in content",
    "Extend theme with primary and secondary colors",
    "Include animation and keyframes for fadeIn, slideUp, etc.",
    "Do NOT include explanations",
    "Do NOT use markdown",
    "Return ONLY valid JavaScript code"
  ]
}
